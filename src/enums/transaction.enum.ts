export enum TransactionEnum {
  DEPOSIT= "DEPOSIT", // depot
  TRANSFER_TO= "TRANSFER_TO" // transfert
}

export enum StatusTrasaction{
  IN_PROGRESS="IN_PROGRESS",
  ACCEPTED="ACCEPTED",
  REJECTED="REJECTED"
}

