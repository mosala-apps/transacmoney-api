import { CalculateTimeRequestInterceptor } from './calculate-time-request.interceptor';

describe('CalculateTimeRequestInterceptor', () => {
  it('should be defined', () => {
    expect(new CalculateTimeRequestInterceptor()).toBeDefined();
  });
});
